import assert from "node:assert/strict";
import fs from "node:fs";

const page = fs.readFileSync(new URL("../src/pages/AdminGroupDivePage.tsx", import.meta.url), "utf8");
const app = fs.readFileSync(new URL("../src/App.tsx", import.meta.url), "utf8");
const nav = fs.readFileSync(new URL("../src/components/layout/AdminLayout.tsx", import.meta.url), "utf8");

assert.match(page, /모집 정보/);
assert.match(page, /옵션 \/ 질문/);
assert.match(page, /신청자/);
assert.match(page, /\["regional", "권역 제안 검토"\]/);
assert.match(page, /onClick=\{\(\) => setActiveTab\("regional"\)\}/);
assert.match(page, /activeTab === "regional"/);
assert.match(page, /regionalRequests\.map/);
assert.match(page, /adminApproveGroupDiveRegionalRequest/);
assert.match(page, /adminRejectGroupDiveRegionalRequest/);
assert.match(page, /최종 권역명/);
assert.match(page, /권역 승인/);
assert.match(page, /회차 \/ 배정/);
assert.match(page, /결제 \/ 환불/);
assert.match(page, /adminCreateGroupDive/);
assert.match(page, /adminAssignGroupDiveSession/);
assert.match(page, /adminRefundGroupDiveApplication/);
assert.match(page, /participantFee: "25000"/);
assert.match(page, /depositAmount: "10000"/);
assert.match(page, /applicationFee: "1000"/);
assert.match(page, /사진 선택/);
assert.match(page, /모임 소개 사진/);
assert.match(page, /MAX_GALLERY_IMAGES/);
assert.match(page, /galleryFiles/);
assert.match(page, /moveGalleryEntry/);
assert.match(page, /removeGalleryEntry/);
assert.match(page, /\+ 권역 추가/);
assert.match(page, /\+ 일정 추가/);
assert.match(page, /대표 일정 · 확정 진행/);
assert.match(page, /시작 시간/);
assert.match(page, /소요 시간/);
assert.match(page, /종료 시간 직접 설정/);
assert.match(page, /\+ 1주 뒤 후보 추가/);
assert.match(page, /\+ 질문 추가/);
assert.match(page, /\+ 선택지 추가/);
assert.match(page, /보증금 보유 중만 보기/);
assert.match(page, /payment\.purpose === "deposit_and_application_fee"/);
assert.match(page, /payment\.status === "paid"/);
assert.match(page, /GENDER_LABELS\[application\.gender\]/);
assert.match(page, />성별</);
assert.match(app, /path="group-dives"/);
assert.match(nav, /path: "\/admin\/group-dives"/);
console.log("Admin Group Dive workspace check passed.");
