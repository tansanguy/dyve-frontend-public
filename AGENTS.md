# DYVE Frontend Agent Instructions

## 한국어 UI 카피

1. UI 카피를 작성하거나 수정하기 전에 사용 가능한 카피·디자인 스킬을 확인하고, 관련 스킬을 먼저 적용한다.
2. 적합한 스킬을 사용할 수 없으면 그 사실을 사용자에게 알리고, `Design.md`의 한국어 카피 규율과 실제 브라우저 검수를 대안으로 제안한다.
3. 설명문은 한 문장에 하나의 주장만 두고, 문장별로 독립된 문단 또는 block으로 분리한다.
4. 긴 문장은 조사·명사구를 자르지 말고, 필요한 쉼표를 포함한 의미 단위로 줄을 나눈다.
5. 서비스명, 금액과 단위, 날짜, 사람 이름은 중간에서 줄을 나누지 않는다.
6. 문자열의 빈 줄이나 `whitespace-pre-line`에 의존하지 않는다. 설명문에는 `break-keep`, 고정 줄에는 block 요소를 사용한다.
7. 이 규율은 설명형 UI 카피에 적용한다. 버튼, 라벨, 토스트, 오류 메시지, 동적 사용자 콘텐츠에는 강제하지 않는다.
8. 수정한 설명문은 320px, 390px, 430px 화면에서 오버플로와 어색한 고립 줄이 없는지 확인한다.
